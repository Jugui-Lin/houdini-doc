// Magic: af3e6b1c
// Automatically generated PSI dialog (PSI version 19.0.561)
// Tue May 31 09:45:35 2022


f_tmp = FLOAT_FIELD;	// dialog.top.ui

group.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    group.label = PARM_LABEL("Group", "group") WIDTH(1.5);
    PARM_FIELD(string, "group") VALUE(group.val) 
	LABELREF(group.label) HSTRETCH;

    group.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    ACTION_ICONBUTTON "BUTTONS_reselect" VALUE(group.script_action)
	HELP("Select geometry from an available viewport.")
	UI_FLAT_ICON_BTN_SIZE_SMALL LOOKFLAT;

    { WIDTH(0.1) }
}

grouptype.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Group Type", "grouptype") WIDTH(1.5);
    { WIDTH(0.09) }

    grouptype.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

sepparm.gad = 

{
    LAYOUT(vertical) HSTRETCH HMARGIN(0.18, 0.1)
    NO_MARGIN_SPACING_SCALE

    { HEIGHT(0.1) }
    SEPARATOR;
    { HEIGHT(0.1) }
}

indextype.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Index Type", "indextype") WIDTH(1.5);
    { WIDTH(0.09) }

    indextype.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

index.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 index.label = PARM_LABEL("Frame Index", "index") WIDTH(1.5);
 PARM(float, 1, "index", none, none) RANGE(0,1)
	 LABELREF(index.label) HSTRETCH;

  { WIDTH(0.1) }
}

bindname1.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    bindname1.label = PARM_LABEL("Attribute Name", "bindname1") WIDTH(1.5);
    PARM_FIELD(string, "bindname1") VALUE(bindname1.val) 
	LABELREF(bindname1.label) HSTRETCH;

    bindname1.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    { WIDTH(0.1) }
}

mode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Mode", "mode") WIDTH(1.5);
    { WIDTH(0.09) }

    mode.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

index_attr_name.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    index_attr_name.label = PARM_LABEL("Output Index Attribute", "index_attr_name") WIDTH(1.5);
    PARM_FIELD(string, "index_attr_name") VALUE(index_attr_name.val) 
	LABELREF(index_attr_name.label) HSTRETCH;

    { WIDTH(0.1) }
}

separator.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("File Path Separator", "separator") WIDTH(1.5);
    { WIDTH(0.09) }

    separator.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

regex.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    regex.label = PARM_LABEL("Regex", "regex") WIDTH(1.5);
    PARM_FIELD(string, "regex") VALUE(regex.val) 
	LABELREF(regex.label) HSTRETCH;

    { WIDTH(0.1) }
}

change_cycle.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("change_cycle", "Set Cycling Mode") toggle VALUE(change_cycle.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

cycle_mode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Cycling Mode", "cycle_mode") WIDTH(1.5);
    { WIDTH(0.09) }

    cycle_mode.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

do_seq_range.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_seq_range", "Override Frame Range") toggle VALUE(do_seq_range.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

new_seq_range.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   new_seq_range.label = PARM_LABEL("New Frame Range", "new_seq_range") WIDTH(1.5);
   PARM(integer, 2, "new_seq_range", none, none, "X,Y")
	RANGE(0,48)  LABELREF(new_seq_range.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

folder1.gad = 

GROUP simple "Cycling" flat 
{ 
    VALUE(folder1.gad.val) LAYOUT(vertical) HSTRETCH 
	    change_cycle.gad;		
	    cycle_mode.gad;		
	    do_seq_range.gad;		
	    new_seq_range.gad;		
} 

folder0.gad = 

GROUP simple "Time" flat 
{ 
    VALUE(folder0.gad.val) LAYOUT(vertical) HSTRETCH 
	    indextype.gad;		
	    index.gad;		
	    bindname1.gad;		
	    mode.gad;		
	    index_attr_name.gad;		
	    separator.gad;		
	    regex.gad;		
} 

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    group.gad; 
    grouptype.gad; 
    sepparm.gad; 
    folder0.gad; 
    folder1.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
